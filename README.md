Create a database connection to mysql server.
After connection to server write username as localhost and password as null respectively and save it in a database var name db.
Then mycursor is selected and database is selected(db).
Then values are inserted into table name record  with two different columns name and email respectively.
Then the cursor mycursor executes the values of sql.
All the necessary changes are committed.
With the help of same operation delete is performed.

